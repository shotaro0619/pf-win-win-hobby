$("#chatchatbox").html("<%= j(render partial: 'rooms/chat', locals:{ message: @message, room: @room, messages: @messages }) %>");
$(".dmbox").val('');

$(function(){
const messageDetails = document.getElementById('chatchatbox');
  function scrollToEnd() {
      messageDetails.scrollTop = messageDetails.scrollHeight;
  }
  scrollToEnd();
 })
