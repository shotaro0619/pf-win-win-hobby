<h1>投稿する</h1>
<%= form_with model: @hobby, local:true do |f| %>
  <h4>趣味名</h4>
  <%= f.text_field :name %>
  <h4>ジャンル</h4>
  <h4>コメント</h4>
  <%= f.text_area :comment %>
  <%= f.submit '投稿' %>
<% end %>